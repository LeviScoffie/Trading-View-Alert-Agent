# Email Notifier Dependencies

# Email
email-validator>=2.0.0

# HTTP API (triggered by external scheduler)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database (sqlite3 is Python built-in, no install needed)

# HTTP client for API calls (if needed)
requests>=2.31.0
aiohttp>=3.8.0

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Templating
jinja2>=3.1.0

# Date/time
python-dateutil>=2.8.0

# AWS SES (optional)
boto3>=1.28.0; extra == "aws"

# SendGrid (optional)
sendgrid>=6.10.0; extra == "sendgrid"

# Logging
structlog>=23.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
